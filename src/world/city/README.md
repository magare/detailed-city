# Rendered City

This folder composes the visible Three.js city.

Do not add new city concepts here first. Add the concept to `src/city`, generate or import its data through `src/generation`, then adapt it into renderable objects here or through `src/city/rendering-handoff`.
