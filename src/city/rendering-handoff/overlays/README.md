# Overlays

Owns data-driven debug and inspection overlay datasets derived from city contracts, validation issues, and object ownership. Overlay modules prepare queryable data for future UI/rendering controls without scraping Three.js mesh names.
