# Data Contracts

Owns shared rules every city object must follow: stable identifiers, geospatial coordinates, level of detail, import/export formats, validation, and metadata.

Use this layer to keep future data imports, procedural generation, and manual authored assets compatible.

The first executable contract module is `cityContracts.ts`. Keep new generated city concepts aligned with those base fields before adding render-specific mesh code.
