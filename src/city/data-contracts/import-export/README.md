# Import Export

Owns import and export contracts for GeoJSON, CityJSON-style data, OSM-inspired features, glTF, CSV, and procedural seeds.

The executable seed is `importExportContracts.ts`. It defines the supported exchange formats, procedural seed JSON export, compact diagnostics, and validation that keeps exported artifacts in local x/z meters without renderer-only state.
